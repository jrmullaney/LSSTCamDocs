The EUPS table
==============

To enable the stack to access your camera's obs\_package, the EUPS
system needs to know about it. To facilitate this, you need to
``declare`` it to EUPS. When you do this, EUPS looks for a table in
the ``ups`` directory you have made in your obs\_package